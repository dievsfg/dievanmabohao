<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.DievMabohao" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="android:statusBarColor">@color/status_bar</item>
        <item name="android:navigationBarColor">@color/background</item>
    </style>

    <style name="Theme.PatternLock" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:statusBarColor">@color/pattern_lock_bg</item>
        <item name="android:navigationBarColor">@color/pattern_lock_bg</item>
        <item name="android:windowBackground">@color/pattern_lock_bg</item>
    </style>

    <!-- Toolbar在暗色模式下使用暗色背景，浅色模式下使用蓝色背景 -->
    <style name="Theme.Toolbar" parent="ThemeOverlay.Material3.Dark.ActionBar">
        <item name="android:background">@color/toolbar_bg</item>
    </style>
</resources>